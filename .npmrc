# CI installs were spending minutes (sometimes stalling past five) inside a
# cache-warm `npm ci`, all of it in npm's registry round-trips: the audit
# report and the funding lookup. Neither affects the build output. Turning
# them off here means local installs behave the same way the workflow does.
audit=false
fund=false
